{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "JWT_SECRET": "@vitacare_jwt_secret",
    "DATABASE_URL": "@vitacare_database_url"
  }
}
